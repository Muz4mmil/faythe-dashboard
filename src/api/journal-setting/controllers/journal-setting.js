'use strict';

/**
 * journal-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::journal-setting.journal-setting');
