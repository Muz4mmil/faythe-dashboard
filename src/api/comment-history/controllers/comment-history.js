'use strict';

/**
 * comment-history controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::comment-history.comment-history');
