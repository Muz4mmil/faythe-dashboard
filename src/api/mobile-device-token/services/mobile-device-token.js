'use strict';

/**
 * mobile-device-token service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mobile-device-token.mobile-device-token');
