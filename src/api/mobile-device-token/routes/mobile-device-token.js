'use strict';

/**
 * mobile-device-token router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mobile-device-token.mobile-device-token');
