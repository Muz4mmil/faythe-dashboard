'use strict';

/**
 * journal-preset-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::journal-preset-question.journal-preset-question');
