'use strict';

/**
 * journal-preset-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::journal-preset-question.journal-preset-question');
