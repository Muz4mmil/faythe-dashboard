'use strict';

/**
 * user-subscription-mapping router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-subscription-mapping.user-subscription-mapping');
